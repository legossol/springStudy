spring:
  datasource:
    url: jdbc:mysql://localhost:3307/root
    driver-class-name: org.mysql.jdbc.Driver
    username: root
    password: root
  jpa:
    open-in-view: false
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: update